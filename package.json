{
  "name": "github-action-echo",
  "version": "1.0.0",
  "description": "GitHub Action for evaluating a shell expression and writing the result to a named output.",
  "author": "Michael Bisgaard Olesen",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bisgardo/github-action-echo.git"
  },
  "main": "index.js",
  "scripts": {
    "build": "eslint . && ncc build index.js -o dist --source-map --license licenses.txt",
    "test": "jest"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "eslint": "^8.40.0",
    "jest": "^29.5.0"
  }
}
